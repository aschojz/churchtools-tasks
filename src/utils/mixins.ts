export const mixins = {
    methods: {
        t: function (key: string, _parameter: string | object) {
            return key;
        },
        escapeHtmlRelaxed(string: string) {
            return string;
        },
    },
};
